#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
using ll = long long;

/*
入力の方法から思い出さないといけない。
*/

int main(){
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  // ---- solve ----
  ll h,b;
  cin >> h >> b;
  cout << h << b << endl;
  return 0;
}
